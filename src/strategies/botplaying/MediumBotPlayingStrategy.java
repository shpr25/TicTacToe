package strategies.botplaying;

import model.Board;
import model.Cell;
import model.Game;

public class MediumBotPlayingStrategy implements BotPlayingStrategy {

    //Block the opponent move if not then place in available space
    @Override
    public Cell makeMove(Board board) {
        return null;
    }
}
